export type Product = {
  title: string;
  description?: string;
  image: string;
  featured?: boolean;
};

export type QualityItem = {
  title: string;
  description: string;
  image: string;
  imageAlt: string;
};
